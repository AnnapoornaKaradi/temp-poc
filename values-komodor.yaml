# values-komodor.yaml — keep secrets out of here
clusterName: "${clusterName}"

# Example optional tweaks (commented until needed)
# resources:
#   requests:
#     cpu: 50m
#     memory: 128Mi
#   limits:
#     cpu: 200m
#     memory: 256Mi

# nodeSelector: {}
# tolerations: []
# affinity: {}

# imagePullSecrets: []
# podLabels:
#   fnf.app: komodor
#   fnf.env: ${clusterName}
